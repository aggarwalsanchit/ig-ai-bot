name: Generate Posts

on:
  schedule:
    # Morning post at 9:00 AM
    - cron: '0 9 * * *'
    # Afternoon post at 2:00 PM  
    - cron: '0 14 * * *'
    # Evening post at 7:00 PM
    - cron: '0 19 * * *'
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          
      - name: Generate post
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
        run: |
          echo "🕐 Generating post at $(date)"
          python scripts/generate_and_notify.py
          
      - name: Commit and push if changes
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add -A
          git diff --staged --quiet || git commit -m "Auto-generated post: $(date +'%Y-%m-%d %H:%M')"
          git push